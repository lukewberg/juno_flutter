import 'package:flutter/material.dart';

class SquareSlide extends StatelessWidget {
  const SquareSlide({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Hello World!'),
    );
  }
}
